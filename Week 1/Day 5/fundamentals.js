/* JS Variable data types 
    numbers
    strings
    boolean
    undefined
    null

    array object/dictionary
*/
/*
    Hierarchy operators
    1) ()
    2) ! 
    3) * / %
    4) + -
    5) > < >= <= == != === !== 
    6) && || 
    7) ++ -- *= /= 
    8) =

*/
var result = ( 10 - 4 + 8 * 3 ) / 2 > 3 && 20/3 < 2 * 8 / 3 ; 
console.log( result );

/* Conditionals 
    if( expression ){

    }

    if( expression ){

    }
    else{

    }

*/

console.log("Result", "8" == 8 );
console.log("Result 2", "8" === 8 );

/* Loops 
 for

 while

 x ++;   x = x + 1;    x += 1;
 x --;   x = x - 1;    x -= 1;
 */

 var nums = [ 10, 20, 30, 40, 50, 60 ];
 console.log( nums.length, nums );
 nums.push( 70, 80 );
 console.log( nums.length, nums );
 nums.pop();
 console.log( nums.length, nums );


 /* shift unshift splice slice */

 /* Functions 
    params + return
    params + no return
    no params + return 
    no params + no return
 */

